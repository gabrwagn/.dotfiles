# Minimal tmux config for opencode.nvim system theme support
# True color support
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",alacritty:Tc"
set -as terminal-features ",*:RGB"
set-environment -g COLORTERM truecolor
